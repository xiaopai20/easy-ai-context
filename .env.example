# Easy AI Context - Example environment (no secrets)
# Copy to .env for local overrides: cp .env.example .env
# Never commit .env (it is in .gitignore).

# ============== DynamoDB (Docker) ==============
DYNAMODB_ENDPOINT=http://dynamodb-local:8000
TABLE_NAME=Context-dev
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=local
AWS_SECRET_ACCESS_KEY=local

# ============== API ==============
PORT=3001
CORS_ORIGIN=*

# Dev mode (bypasses Cognito auth)
DEV_MODE=true
DEV_USER_ID=dev-user-123
DEV_EMAIL=dev@example.com
DEV_NAME=Dev User

# Access control (comma-separated emails; empty = allow all)
ALLOWED_EMAILS=dev@example.com

# ============== Scripts (host) ==============
# When running scripts on host: DYNAMODB_ENDPOINT=http://localhost:8010
# API_BASE_URL=http://localhost:3010
